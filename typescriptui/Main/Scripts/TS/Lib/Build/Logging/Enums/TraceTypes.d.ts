declare module TSUI.Logging {
    /** The internal list of trace types for logs. */
    enum TraceTypes {
        LOG,
        COMMON,
        LOGIC_TRACE,
        DATA_TRACE,
        ERROR_TRACE,
    }
}
